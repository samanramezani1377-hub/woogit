# WooGit

اپلیکیشن اندرویدی اختصاصی برای مدیریت سریع و آفلاین‌محور فروشگاه‌های ووکامرس، با تمرکز بر سه نیاز اصلی صاحب فروشگاه: **دریافت فوری اعلان سفارش جدید، مشاهده و مدیریت روزانه سفارش‌ها، و مدیریت سریع محصولات**.

---

## پرامپت پروژه (Project Prompt)

> این بخش یک شرح کامل و دقیق از چیزی است که باید ساخته شود. هر بار که قرار است روی این پروژه (توسط انسان یا مدل هوش مصنوعی) کار شود، باید این پرامپت به‌طور کامل خوانده شود.

> **قاعده‌ی تفسیر «آینده» در این سند:** هر جا در این پرامپت یا در جلسات بعدی گفته شود که قابلیتی «در آینده» اضافه می‌شود، منظور این است که **هسته (Core) باید از همین حالا برای پذیرفتن آن قابلیت آماده و باز طراحی شده باشد** (مثلاً ماژول جدا، لایه‌ی انتزاعی، اینترفیس مشخص)، اما **پیاده‌سازی و فعال‌سازی واقعی آن قابلیت به نسخه‌های بعدی موکول می‌شود** و بخشی از نسخه‌ی یک نیست.

### ۱. تعریف محصول و مشکل اصلی

WooGit قرار نیست صرفاً یک اپ دیگر برای مشاهده‌ی WooCommerce باشد. هدف آن ساخت یک ابزار مدیریتی سریع و تخصصی برای صاحب فروشگاه است تا برای عملیات اصلی و پرتکرار فروشگاه، تا جای ممکن از پنل WordPress/WooCommerce بی‌نیاز شود.

سه محور اصلی محصول عبارت‌اند از:

1. **اعلان سفارش جدید — محور فوری (Immediate):** کاربر باید از ایجاد سفارش جدید در سریع‌ترین زمان ممکن مطلع شود. این قابلیت نقطه‌ی تماس اولیه‌ی کاربر با فروشگاه و از نظر فوریت مهم‌ترین قابلیت WooGit است. هدف عملی این است که کاربر برای مطلع شدن از سفارش جدید نیاز به باز کردن مداوم سایت یا اپ نداشته باشد؛ فوریت آن در مقیاس دقیقه تا حداکثر حدود یک ساعت تعریف می‌شود.
2. **مشاهده و مدیریت سفارش — محور روزانه (Daily):** پس از دریافت اعلان، کاربر باید بتواند اطلاعات کامل سفارش را داخل برنامه مشاهده و عملیات موردنیاز روزانه را انجام دهد. این بخش باید برای استفاده‌ی ساعتی تا روزانه سریع، واضح و بدون نیاز به ورود به پنل WordPress باشد.
3. **افزودن و اصلاح محصولات — محور کم‌فوریت‌تر اما سرعت‌محور (Weekly / On-demand):** کاربر ممکن است این بخش را روزانه، چند بار در هفته یا حتی فقط هفته‌ای یک‌بار استفاده کند؛ اما وقتی به آن نیاز دارد، فرآیند افزودن یا ویرایش محصول باید تا حد امکان سریع، ساده و با کمترین تعداد لمس انجام شود. هدف این بخش حذف نیاز به مراجعه به پنل WordPress برای مدیریت معمول محصولات است.

بنابراین WooGit سه قابلیت را با یک سطح اهمیت یکسان نمی‌بیند. **فوریت و الگوی استفاده‌ی هر قابلیت متفاوت است و معماری، UX، اعلان‌ها، همگام‌سازی و بهینه‌سازی باید این تفاوت را از ابتدا در نظر بگیرند.**

### ۲. اولویت‌های اصلی (به ترتیب اهمیت)

۱. **اطلاع سریع از سفارش جدید** — کاربر نباید برای فهمیدن اینکه سفارشی ثبت شده یا نه، مجبور به باز کردن اپ یا سایت باشد.

۲. **دسترسی سریع به سفارش و اطلاعات کامل آن** — اعلان باید به یک مسیر سریع برای مشاهده‌ی سفارش منجر شود و کاربر بتواند عملیات معمول سفارش را داخل اپ انجام دهد.

۳. **سرعت افزودن و اصلاح محصول** — اگرچه این قابلیت معمولاً فوریت کمتری نسبت به سفارش دارد، اما در زمان استفاده باید با کمترین زمان و کمترین تعداد لمس انجام شود.

۴. **حداکثر بی‌نیازی از پنل WordPress/WooCommerce برای کارهای روزمره** — کاربر نباید برای عملیات اصلی تعریف‌شده در WooGit مجبور به باز کردن مرورگر یا پنل مدیریت شود.

۵. **مدل همگام‌سازی سریع و آفلاین‌محور** — تغییرات قابل انجام در اپ باید تا حد امکان ابتدا به‌صورت محلی ثبت و UI فوراً به‌روزرسانی شود و سپس با سرور همگام شود.

۶. **سرعت اجرای اپ در حد اپ‌های بسیار سریع و بهینه‌ی بازار** — تجربه‌ی کاربری باید native و روان باشد، بدون تأخیر و لگ، با تمرکز ویژه روی مسیرهای پرتکرار و فوری.

۷. **قابلیت تبدیل بعدی به یک محصول تجاری (Productization)** — معماری باید از ابتدا به‌گونه‌ای طراحی شود که بتوان بعداً آن را برای چندین فروشگاه/چندین کاربر عرضه کرد، مدل Freemium/Premium داشت و بدون بازنویسی اساسی مقیاس‌پذیر بود.

### ۳. معماری فنی پیشنهادی

**اصل بنیادین: ساخت از «هسته» به بیرون (Core-Out Architecture).**
پروژه نباید مستقیم از UI اندروید شروع شود. باید یک **هسته‌ی مشترک (Core Module)** مستقل از پلتفرم ساخته شود که شامل تمام منطق تجاری، مدل‌های دامنه، موتور همگام‌سازی (Sync Engine)، صف تغییرات، و ارتباط با REST API ووکامرس باشد. لایه‌های بیرونی (UI اندروید، و بعداً UI آی‌فون) فقط مصرف‌کننده‌ی این هسته هستند. مزیت این رویکرد:
- وقتی هسته آپدیت یا اصلاح می‌شود (باگ‌فیکس، قابلیت جدید، بهبود موتور Sync)، این تغییر خودکار به تمام پلتفرم‌ها می‌رسد.
- افزودن قابلیت‌های آینده (پشتیبانی هوش مصنوعی، گیت‌وی‌های پرداخت جدید، منطق جدید کسب‌وکار) در هسته انجام می‌شود و تنها یک بار پیاده‌سازی می‌شود.

- **رویکرد Multiplatform:** به‌جای نوشتن هسته فقط برای اندروید، از **Kotlin Multiplatform (KMP)** استفاده شود. لایه‌ی هسته (دامنه، شبکه، دیتابیس محلی، موتور Sync) با KMP نوشته می‌شود تا هم اندروید و هم (در آینده) iOS بتوانند مستقیماً از همان کد استفاده کنند، بدون بازنویسی منطق تجاری. این دقیقاً همان آماده‌بودن برای iOS است که خواسته شده — فعلاً فقط لایه‌ی UI اندروید (Compose) روی این هسته ساخته می‌شود؛ لایه‌ی UI iOS (SwiftUI) هر وقت لازم شد بدون دست‌زدن به هسته اضافه می‌شود.
- **UI اندروید:** Jetpack Compose (Material 3) — با تم اختصاصی «Liquid Glass» (بخش ۷)
- **معماری لایه‌ای:** Clean Architecture (Core مشترک KMP + لایه‌ی Presentation مخصوص هر پلتفرم)، مشابه رویکرد پروژه‌ی ZeroGram اما با جداسازی سخت‌گیرانه‌تر هسته از UI
- **دیتابیس محلی/صف تغییرات:** SQLDelight یا Room با پشتیبانی KMP — تمام تغییرات کاربر ابتدا اینجا ذخیره می‌شوند (منبع حقیقت آفلاین)
- **ارتباط شبکه:** Ktor Client (سازگار با KMP) برای صحبت با REST API ووکامرس (`wp-json/wc/v3/...`)
- **همگام‌سازی پس‌زمینه:** WorkManager در اندروید (فراخوانی منطق Sync از هسته‌ی مشترک)
- **تزریق وابستگی:** Koin یا Hilt (بسته به سازگاری با KMP)
- **Async:** Kotlin Coroutines & Flow
- **ماژول جداگانه برای هوش مصنوعی:** از همین ابتدا یک ماژول (حتی خالی/placeholder) به اسم `ai` یا `assistant` در نظر گرفته شود تا وقتی قابلیت‌های هوش مصنوعی (مثلاً پیشنهاد قیمت، تولید توضیحات محصول، تحلیل فروش) اضافه شدند، به هسته‌ی اصلی وصل شوند بدون تغییر ساختار کلی.
- بدون نیاز به سرور اختصاصی برای هسته‌ی اصلی — خود وردپرس/ووکامرس نقش بک‌اند را دارد. (سرور جدا فقط برای لایسنس/پرمیوم طبق بخش ۶)

### ۴. مکانیزم نسخه‌بندی داده (Data Versioning / «شماره کامیت»)

> این مکانیزم بخشی از هسته‌ی نسخه‌ی یک است، نه یک قابلیت آینده — چون مستقیماً به اولویت سرعت و آفلاین‌محور بودن پروژه وابسته است.

ایده: یک شماره‌ی نسخه‌ی سراسری (شبیه به شماره‌ی کامیت در گیت) برای داده‌های فروشگاه نگه‌داری شود:

- هر بار داده‌ای در سایت تغییر می‌کند (چه از طریق خود اپ، چه از پنل وردپرس، چه از هر منبع دیگری)، یک شماره‌ی نسخه‌ی سراسری در سمت سرور **بالا می‌رود**.
- اپ همیشه آخرین شماره‌نسخه‌ای که خودش دارد را به‌همراه داده‌ها به‌صورت محلی (کاملاً داخل گوشی) ذخیره می‌کند. تمام صفحات اپ از همین داده‌ی محلی خوانده می‌شوند، نه مستقیم از سایت.
- در دو لحظه، اپ فقط **شماره‌ی نسخه** را از سایت می‌پرسد (یک درخواست بسیار سبک، نه کل داده):
  1. در پس‌زمینه، حتی وقتی اپ بسته است (با WorkManager/کارهای دوره‌ای)
  2. لحظه‌ی باز شدن اپ (یا حتی کمی قبل از باز شدن کامل، اگر پلتفرم اجازه دهد)
- اگر شماره‌نسخه‌ی سرور با نسخه‌ی محلی یکسان بود → هیچ داده‌ای از سایت لود نمی‌شود، همان داده‌ی محلی نمایش داده می‌شود.
- اگر شماره‌نسخه فرق داشت → همگام‌سازی داده (کامل یا فقط بخش تغییریافته) در پس‌زمینه انجام می‌شود.
- وقتی خود اپ تغییری را Push می‌کند، بعد از موفقیت‌آمیز بودن ارسال، هم سرور شماره‌نسخه را بالا می‌برد و هم اپ نسخه‌ی محلی‌اش را هم‌گام می‌کند.

**نکته‌ی پیاده‌سازی:** خود API استاندارد ووکامرس چنین شماره‌ی سراسری‌ای ندارد (فقط `date_modified` روی هر آیتم دارد). دو مسیر ممکن است:
1. **راه ساده‌تر (بدون افزونه‌ی اضافه):** به‌جای شماره‌ی صحیح ساده، از جدیدترین `date_modified_gmt` در میان محصولات/سفارش‌ها به‌عنوان «نسخه» استفاده شود.
2. **راه دقیق‌تر و پیشنهادی:** یک افزونه‌ی همراه سبک روی وردپرس ساخته شود که با قلاب‌شدن (hook) به رویدادهای تغییر محصول/سفارش/موجودی، یک شمارنده‌ی صحیح را در دیتابیس وردپرس بالا ببرد و از طریق یک endpoint اختصاصی و بسیار سبک (`GET /wp-json/woogit/v1/version`) در دسترس اپ بگذارد. (این افزونه طبق بخش لایسنس، چون مستقیم با هسته‌ی وردپرس یکپارچه می‌شود، باید GPLv2+ باشد.)

### ۵. نکات فنی و محدودیت‌های شناخته‌شده‌ی API ووکامرس

این‌ها باید در طراحی و پیاده‌سازی در نظر گرفته شوند:

- ارسال دسته‌جمعی (batch) از طریق `POST /wp-json/wc/v3/products/batch` با کلیدهای `create` / `update` / `delete` انجام می‌شود.
- محدودیت پیش‌فرض هر درخواست batch **۱۰۰ آیتم** است (قابل افزایش با فیلتر سمت سرور `woocommerce_api_bulk_limit`، چون هاست فروشگاه در اختیار خودمان است).
- آپلود تصویر محصول در batch API انجام نمی‌شود؛ باید دو مرحله‌ای انجام شود: ابتدا آپلود عکس به `wp-json/wp/v2/media`، سپس اتصال شناسه‌ی عکس به محصول.
- REST API ووکامرس ذاتاً pull-based است، نه push. برای اطلاع لحظه‌ای از رویدادهایی مثل سفارش جدید، باید از **WooCommerce Webhooks** یا مکانیزم مناسب اعلان/Push استفاده شود.
- احراز هویت برای یک فروشگاه: Consumer Key/Secret. برای نسخه‌ی چندفروشگاهی/تجاری آینده، نیاز به مکانیزم اتصال چندگانه (مثلاً OAuth یا مدیریت چند اتصال هم‌زمان) خواهد بود.
- سرعت واقعی همگام‌سازی به قدرت هاست فروشگاه وابسته است، نه فقط به کد اپ؛ در batchهای بزرگ روی هاست ضعیف احتمال timeout وجود دارد و باید مدیریت خطا/تلاش مجدد (retry) در نظر گرفته شود.

### ۶. مسیر تجاری‌سازی و بیلینگ (برای آینده)

از آن‌جا که مخاطب اصلی داخل ایران است، **تکیه‌ی صرف بر Google Play Billing کافی نیست**. معماری بیلینگ باید از ابتدا **چندگانه و قابل تعویض (Pluggable Billing)** طراحی شود:

- **Google Play Billing**
- **کافه‌بازار (Cafe Bazaar IAB)** و **مایکت (Myket IAB)**
- **درگاه‌های پرداخت مستقیم ایرانی** مثل زرین‌پال، زیبال، آیدی‌پی، پی‌پینگ
- انتخاب روش پرداخت برای هر کانال توزیع باید از طریق یک abstraction/config انجام شود، نه با وابستگی مستقیم قابلیت‌های تجاری به یک فروشگاه اپ خاص.
- برای مدیریت لایسنس/اشتراک کاربران در مقیاس چندفروشگاهی، یک بک‌اند سبک برای تأیید و مدیریت اشتراک در آینده قابل استفاده خواهد بود؛ داده‌های فروشگاه‌ها همچنان روی هاست خود کاربر باقی می‌مانند.
- تخمین زمان ساخت نسخه‌ی اولیه (MVP شامل: اعلان سفارش، مشاهده/مدیریت سفارش، محصول، صف آفلاین و Batch Sync) فعلاً صرفاً تخمین اولیه است و پس از نهایی‌شدن مشخصات محصول باید بازبینی شود.

### ۷. رابط کاربری: تم Liquid Glass

نمای کلی اپ باید از سبک طراحی **Liquid Glass** (شیشه‌ی مایع — سبک طراحی شفاف/لایه‌ای با بلور و نور، مشابه جدیدترین زبان طراحی اپل) پیروی کند:
- لایه‌های نیمه‌شفاف با افکت بلور پشت زمینه (Backdrop Blur)
- سایه‌ها و هایلایت‌های نرم برای حس «شیشه‌ای بودن» و عمق
- انیمیشن‌های نرم و روان هنگام جابه‌جایی بین لایه‌ها
- در Jetpack Compose این جلوه از طریق ترکیب `Modifier.blur`، شفافیت رنگی (alpha)، و گرادیان‌های ظریف قابل پیاده‌سازی است؛ باید یک سیستم طراحی (Design System) اختصاصی برای این تم در ابتدای کار تعریف شود (رنگ‌ها، شعاع بلور، شفافیت استاندارد) تا در کل اپ یکدست اجرا شود.

### ۸. آماده‌بودن برای آینده (Future-Proofing)

ساختار پروژه از روز اول باید طوری باشد که افزودن موارد زیر بدون بازطراحی اساسی ممکن باشد:
- **پشتیبانی از هوش مصنوعی** — از طریق ماژول جداگانه `ai` که به هسته وصل می‌شود.
- **iOS** — از طریق هسته‌ی مشترک KMP؛ فقط لایه‌ی UI (SwiftUI) بعداً اضافه می‌شود.
- **به‌روزرسانی هسته** — نسخه‌بندی مشخص برای ماژول Core تا آپدیت‌های آن مستقل از UI پلتفرم‌ها منتشر و اعمال شوند.
- **درگاه‌های پرداخت جدید** — از طریق لایه‌ی انتزاعی Billing.
- **گسترش مدیریت فروشگاه** — قابلیت‌های آینده نباید باعث شوند سه محور اصلی محصول (اعلان سفارش، سفارش‌ها و مدیریت سریع محصولات) تحت‌الشعاع قرار بگیرند.

### ۹. آنچه هنوز باید مشخص شود (برای جلسات بعدی)

این بخش عمداً تا زمان تصمیم‌گیری مشترک تکمیل نمی‌شود. موارد اصلی شامل:

- تعریف دقیق رفتار اعلان سفارش جدید و سطح فوریت/قابلیت اطمینان آن
- تعیین عملیات دقیق قابل انجام روی سفارش و ترتیب اهمیت آن‌ها
- طراحی دقیق مسیر مشاهده‌ی سفارش از اعلان تا جزئیات سفارش
- طراحی دقیق UI/UX صفحه‌ی افزودن سریع محصول و ویرایش محصول
- نحوه‌ی دقیق نمایش صف تغییرات pending به کاربر
- مدیریت تعارض (conflict) در صورت تغییر هم‌زمان از پنل وردپرس
- طراحی دقیق مدل Premium
- تعریف دقیق سیستم طراحی Liquid Glass
- انتخاب نهایی بین Room و SQLDelight برای دیتابیس محلی در معماری KMP

---

## لایسنس

با توجه به هدف تجاری‌سازی و ارائه‌ی نسخه‌ی پرمیوم در آینده، پیشنهاد لایسنس به شرح زیر است:

- **اپلیکیشن اندروید (این ریپازیتوری):** چون این اپ صرفاً از طریق REST API عمومی با ووکامرس/وردپرس صحبت می‌کند و هیچ کد GPL از هسته‌ی وردپرس/ووکامرس در آن استفاده یا کپی نمی‌شود، این اپ **اثر مشتق (derivative work)** محسوب نمی‌شود و می‌تواند تحت هر لایسنسی از جمله لایسنس‌های تجاری/انحصاری منتشر شود. برای شروع، **All Rights Reserved (بدون لایسنس متن‌باز)** یا **Business Source License (BUSL-1.1)** پیشنهاد می‌شود — دومی این امکان را می‌دهد که کد بعداً پس از مدتی به‌صورت خودکار متن‌باز شود، بدون این‌که رقبا بلافاصله از آن نسخه‌برداری تجاری کنند.
- **اگر بعداً یک افزونه‌ی همراه برای خود وردپرس ساخته شود** (مثلاً برای Webhookهای اختصاصی یا endpointهای سفارشی) و قرار باشد از طریق مخزن رسمی wordpress.org توزیع شود، آن بخش باید طبق قوانین wordpress.org حتماً **GPLv2 یا بالاتر** باشد، چون به‌طور مستقیم با هسته‌ی وردپرس/ووکامرس (که خود GPL هستند) یکپارچه می‌شود. این محدودیت فقط شامل آن افزونه می‌شود، نه اپ اندروید.

---

*این فایل فعلاً سند زنده‌ی تعریف محصول است. کدنویسی هنوز شروع نشده و مشخصات آن مرحله‌به‌مرحله با تصمیم‌های پروژه تکمیل خواهد شد.*
