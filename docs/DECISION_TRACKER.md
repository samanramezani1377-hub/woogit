# ووگیت — پیگیری تصمیم‌ها و پرسش‌ها

این فایل فهرست زنده‌ی تصمیم‌ها و پرسش‌های محصول و معماری است. پیش از طرح هر پرسش جدید باید این فایل بررسی شود تا پرسش تکراری مطرح نشود.

## تصمیم قطعی — نسخه تغییرات سمت فروشگاه در نسخه اول

- [x] در نسخه اول برای تشخیص نسخه/آخرین تغییر هر موجودیت از `date_modified_gmt` ووکامرس استفاده می‌شود.
- [x] این مقدار «نسخه تشخیصی سمت فروشگاه» محسوب می‌شود، نه یک شناسه Commit واقعی و یکتای تاریخچه تغییرات.
- [x] برای هر موجودیت، شناسه موجودیت به‌همراه `date_modified_gmt` به‌عنوان وضعیت نسخه سمت فروشگاه نگهداری می‌شود.
- [x] اگر مقدار `date_modified_gmt` سمت فروشگاه نسبت به وضعیت شناخته‌شده محلی تغییر کرده باشد، برنامه آن را به‌عنوان نسخه جدیدتر تشخیص می‌دهد.
- [x] Commit واقعی و تاریخچه دقیق تغییرات به نسخه‌های آینده، پس از امکان استفاده از افزونه همراه یا سرور اختصاصی، موکول می‌شود.
- [x] این لایه باید طوری طراحی شود که در آینده بتوان `date_modified_gmt` را بدون بازطراحی Core با شناسه Commit واقعی جایگزین یا تکمیل کرد.

## تصمیم قطعی — اعلان سفارش جدید در نسخه اول

- [x] مبنای نسخه اول برای تشخیص سفارش جدید، بررسی دوره‌ای و همگام‌سازی پس‌زمینه‌ای با رابط برنامه‌نویسی ووکامرس است.
- [x] برنامه باید برای اجرای این بررسی‌ها از سازوکارهای رسمی و کم‌مصرف اندروید مانند WorkManager استفاده کند و زمان‌بندی را با محدودیت‌های سیستم‌عامل تطبیق دهد.
- [x] برنامه نباید برای دریافت اعلان، یک اتصال دائمی یا سرویس پس‌زمینه دائمی ایجاد کند.
- [x] در صورت وجود یک سرویس خارجی قابل‌اعتماد که بدون راه‌اندازی سرور اختصاصی ووگیت و بدون افزونه وردپرس بتواند اعلان سفارش جدید را به‌صورت قابل‌اتکا بهتر از بررسی دوره‌ای فراهم کند، در بررسی فنی آینده امکان استفاده از آن بررسی می‌شود.
- [x] استفاده از سرویس خارجی نباید باعث وابستگی اجباری به Backend اختصاصی ووگیت در نسخه اول شود.
- [x] در حال حاضر سرویس‌های اعلان فوری مانند Firebase Cloud Messaging برای ارسال خودکار از سمت فروشگاه به یک محیط مورداعتماد برای ارسال پیام نیاز دارند؛ بنابراین به‌تنهایی جایگزین بررسی دوره‌ای بدون Backend/افزونه نیستند.
- [x] اگر در آینده Backend یا افزونه اضافه شود، معماری اعلان باید بتواند از اعلان Push واقعی استفاده کند، بدون اینکه لایه اصلی تشخیص سفارش بازطراحی شود.
- [x] اطلاعات اعلان سفارش جدید در نسخه اول باید شامل شماره سفارش، مبلغ، تعداد کالا، نام کالاها، تاریخ و ساعت باشد.

## تصمیم قطعی — آمادگی کامل راست‌به‌چپ و چپ‌به‌راست

- [x] نسخه اول فارسی و راست‌به‌چپ است.
- [x] رابط کاربری باید از ابتدا برای تغییر کامل جهت بین راست‌به‌چپ (RTL) و چپ‌به‌راست (LTR) طراحی شود.
- [x] در طراحی رابط کاربری نباید چیدمان به مختصات ثابت چپ/راست وابسته باشد؛ در موارد ممکن از مفاهیم شروع/پایان استفاده شود.
- [x] فاصله‌ها، ترازها، فرم‌ها، فهرست‌ها، منوها، آیکون‌ها و اجزای رابط نباید با تغییر جهت نوشتار به‌هم بریزند.
- [x] انیمیشن‌ها و رفتارهای حرکتی نیز باید با تغییر جهت قابل تطبیق باشند.
- [x] طول متفاوت متن در زبان‌های آینده نباید باعث خراب شدن چیدمان شود.
- [x] اضافه شدن زبان‌های چپ‌به‌راست در آینده نباید نیازمند بازنویسی رابط کاربری یا منطق اصلی باشد.
- [x] ساختار ترجمه باید از ابتدا قابل توسعه برای زبان‌ها و جهت‌های نوشتاری مختلف باشد.

## وضعیت

- `[x]` تصمیم قطعی و در مستندات پروژه ثبت شده است.
- `[ ]` پرسش باز و نیازمند تصمیم کاربر است.
- **مسدودکننده**: موضوعی که پیش از شروع کدنویسی باید حتماً مشخص شود.
- **نیازمند بررسی فنی**: موضوعی که باید با بررسی فنی یا مستندات مشخص شود، نه با حدس.

## تصمیم‌های قطعی

- [x] سه محور اصلی: اعلان سفارش جدید، سفارش‌ها، مدیریت محصولات.
- [x] قابلیت‌های قبلی حذف نمی‌شوند و در اولویت‌های بعدی باقی می‌مانند.
- [x] اعلان سفارش جدید بالاترین فوریت را دارد؛ هدف اطلاع‌رسانی در مقیاس دقیقه و حداکثر قابل‌قبول حدود یک ساعت است.
- [x] اعلان باید حتی با بسته بودن برنامه دریافت شود و اطلاعات اصلی آن شامل شماره سفارش، مبلغ، تعداد کالا، نام کالاها، تاریخ و ساعت باشد.
- [x] تغییرات وضعیت سفارش برای آینده آماده می‌شوند و در نسخه اول محور اصلی اعلان فقط سفارش جدید است.
- [x] داشبورد نسخه اول فقط تعداد سفارش‌های جدید را نشان می‌دهد.
- [x] سفارش: اطلاعات مهم در صفحه اصلی و جزئیات کامل و کم‌کاربردتر در «ویرایش سفارش» قرار می‌گیرند.
- [x] عملیات پرتکرار قابل انجام مستقیم در صفحه اصلی سفارش: تغییر وضعیت سفارش و یادداشت سریع.
- [x] تمام فیلدهای «ویرایش کامل سفارش» در نسخه اول فعال هستند (مشتری، آدرس، اقلام و تعداد، تخفیف، حمل، پرداخت، رهگیری، یادداشت‌ها).
- [x] محصول متغیر (Variable Product) و Variationها به‌طور کامل (ساخت/ویرایش/حذف) در V1 پشتیبانی می‌شوند.
- [x] ویژگی‌های محصول (Attributes) به‌صورت کامل در V1 پشتیبانی می‌شوند: هم سفارشی (Custom) و هم سراسری (Global).
- [x] گالری تصاویر محصول در V1 بدون محدودیت خاص تعداد/حجم است (فقط محدودیت طبیعی حافظه/شبکه) و تصاویر بدون فشرده‌سازی/تغییر اندازه با کیفیت اصلی آپلود می‌شوند.
- [x] حذف محصول در V1 فعال است.
- [x] لغو و حذف سفارش هر دو در V1 فعال هستند.
- [x] به‌روزرسانی اپ نباید دیتابیس محلی/صف Pending را پاک کند؛ اما با تعویض/فرمت گوشی هیچ بک‌آپ ابری خودکاری برای بازیابی آن‌ها استفاده نمی‌شود (کاربر باید دوباره متصل شود). Credentialهای Keystore در هر حالت از بک‌آپ مستثنی هستند.
- [x] نمایش وضعیت سنک به کاربر در V1 فقط سه حالت ساده است: معلق/همگام‌شده/ناموفق (بدون تاریخچه کامل تغییرات).
- [x] خطای احراز هویت: پیام کوتاه + دکمه تلاش مجدد.
- [x] خطای شبکه/موقت: Retry خاموش در پس‌زمینه + نشانگر کوچک رنگی وضعیت اتصال (سبز/زرد/قرمز) بالای صفحه.
- [x] خطای سطح دسترسی (Permission): کنترل مربوطه فقط پنهان می‌شود، بدون پیام جداگانه.
- [x] خطای اعتبارسنجی فرم: فوراً زیر همان فیلد مشکل‌دار نمایش داده می‌شود.
- [x] Conflict غیرقابل ادغام خودکار: مودال با نمایش مقدار Local و Server برای انتخاب کاربر.
- [x] خطای سرور (5xx): مانند خطای شبکه، Retry خاموش در پس‌زمینه + نشانگر وضعیت اتصال.
- [x] سطح آزمایش اجباری V1: Unit + Integration + UI Test کامل، به‌همراه تست الزامی شبکه ناپایدار/کند (قطعی حین تغییر، Retry، Idempotency).
- [x] قبل از کدنویسی، تمام صفحات/جریان‌های V1 باید در سطح Hi-Fi طراحی شوند، همراه با Design System کامل تم Liquid Glass.
- [x] جستجوی محصول از نسخه اول لازم است؛ جستجوی سراسری برای آینده است.
- [x] افزودن محصول یک جریان واحد از افزودن سریع تا ساخت کامل است؛ فقط نام اجباری است و تصویر اصلی مهم ولی اختیاری است.
- [x] نسخه اول بدون سرور اختصاصی ووگیت است.
- [x] نسخه اول اتصال مستقیم به رابط برنامه‌نویسی ووکامرس دارد.
- [x] افزونه همراه وردپرس به نسخه‌های آینده موکول شده است.
- [x] احراز هویت باید با امنیت متناسب، ذخیره‌سازی امن/کلیدنگار اندروید، ارتباط امن و جلوگیری از نشت اطلاعات ورود انجام شود.
- [x] کاربر برای اتصال اصلی با آدرس فروشگاه + کلید مصرف‌کننده + رمز مصرف‌کننده کار می‌کند.
- [x] نسخه اول یک فروشگاه است؛ هسته از ابتدا برای چندفروشگاهی آماده است.
- [x] نسخه اول یک دستگاه و یک نقش/سطح دسترسی است.
- [x] آینده: چند کاربر و چند دستگاه، با استقلال هر دستگاه.
- [x] اینترنت ضعیف یا قطع کوتاه نباید برنامه را از کار بیندازد؛ صف عملیات معلق، تلاش مجدد با فاصله و ادامه پس از اتصال لازم است.
- [x] نسخه اول کاملاً آفلاین نیست، ولی هسته باید برای حالت آفلاین کامل در آینده آماده باشد.
- [x] داده‌های مفید سفارش و محصول به‌صورت محلی نگهداری می‌شوند؛ اطلاعات ورود جدا و امن نگهداری می‌شوند.
- [x] هر موجودیتی که تغییر کند، نسخه/شماره تغییر مخصوص خودش را افزایش می‌دهد.
- [x] تغییر محلی بلافاصله رابط کاربری را به‌روز می‌کند و سپس برای همگام‌سازی وارد صف عملیات معلق می‌شود.
- [x] اگر همگام‌سازی شکست بخورد، تغییر از بین نمی‌رود و بعداً دوباره ارسال می‌شود.
- [x] تعارض در آینده: ابتدا ادغام خودکار در سطح فیلد؛ اگر امن و قابل ادغام نبود، تصمیم‌گیری توسط کاربر.
- [x] ردیابی کاربر، مرکز اعلان زنده و دستیار هوشمند/دستیار سایت باید از ابتدا مرز معماری داشته باشند، اما در نسخه اول فعال نمی‌شوند.
- [x] تمام متن‌ها و نوشته‌های قابل مشاهده در نسخه اول فارسی هستند.
- [x] نسخه اول راست‌چین و راست‌به‌چپ است.
- [x] معماری رابط کاربری و ترجمه از ابتدا برای افزودن زبان‌های دیگر آماده است.

## پرسش‌های باز و مواردی که باید قبل از کدنویسی تعیین شوند

### پرسش ۰۱ — حل شد

نسخه تغییرات سمت فروشگاه با `date_modified_gmt` مشخص شد. تصمیم نهایی در بالا ثبت شده است.

### پرسش ۰۲ — حل شد

مبنای نسخه اول بررسی دوره‌ای و همگام‌سازی پس‌زمینه‌ای است. امکان استفاده از سرویس خارجی اعلان در صورت پیدا شدن راهکار سازگار با محدودیت‌های نسخه اول در بررسی فنی آینده باز گذاشته شد.

### پرسش ۰۳ — حل شد

عملیات پرتکراری که مستقیماً در صفحه اصلی سفارش (لایه اول) قابل انجام هستند: **تغییر وضعیت سفارش** و **افزودن/مشاهده یادداشت سریع**. سایر عملیات (تغییر اطلاعات مشتری، آدرس، اقلام، تخفیف، هزینه حمل، روش پرداخت، اطلاعات ارسال/رهگیری و غیره) در «ویرایش کامل سفارش» باقی می‌مانند.

### پرسش ۰۴ — حل شد

تمام فیلدهای «ویرایش کامل سفارش» (لایه دوم) در نسخه اول فعال هستند: تغییر اطلاعات مشتری، تغییر آدرس، تغییر اقلام و تعداد، تخفیف‌ها، هزینه حمل، روش پرداخت، اطلاعات ارسال/رهگیری و یادداشت‌ها. هیچ‌کدام به نسخه‌های بعدی موکول نمی‌شوند.

### پرسش ۰۵ — حل شد

- محصول متغیر (Variable Product) و Variationها: پشتیبانی **کامل** از V1 — ساخت، ویرایش و حذف محصول متغیر و Variationهای آن (نه فقط مشاهده).
- ویژگی‌ها (Attributes): پشتیبانی **کامل** از V1 — هم ویژگی‌های سفارشی (Custom) مخصوص یک محصول و هم ویژگی‌های سراسری (Global attributes) قابل استفاده در چند محصول.
- محصول ساده (Simple Product) نیز کامل پشتیبانی می‌شود (طبق ORDERS_AND_PRODUCTS.md).
- موجودی (Stock)، دسته‌بندی‌ها و تصاویر برای هر دو نوع محصول (ساده و متغیر) در V1 فعال هستند.

### پرسش ۰۶ — حل شد

- محدودیت تعداد/حجم گالری تصاویر: **بدون محدودیت خاص از سمت اپ**؛ فقط محدودیت طبیعی حافظه دستگاه و شبکه اعمال می‌شود.
- فشرده‌سازی/تغییر اندازه: تصویر **بدون تغییر و با کیفیت اصلی** آپلود می‌شود؛ اپ پیش از آپلود آن را فشرده یا کوچک نمی‌کند.
- بارگذاری، تلاش مجدد (Retry) و مدیریت اینترنت ضعیف طبق اصول عمومی `OFFLINE_QUEUE_AND_PUSH.md` و `OFFLINE_AND_RESILIENCE.md` انجام می‌شود (صف Pending، Retry با تأخیر، عدم گم‌شدن تغییر).

### پرسش ۰۷ — حل شد

- حذف محصول: **فعال در V1** (از طریق WooCommerce API؛ رفتار پیش‌فرض ووکامرس معمولاً انتقال به زباله‌دان/Trash است که امکان بازیابی را فراهم می‌کند).
- حذف/لغو سفارش: **فعال در V1** — هم لغو سفارش (تغییر وضعیت به Cancelled) و هم حذف واقعی سفارش پشتیبانی می‌شود.
- هر دو عملیات باید مانند سایر تغییرات از مدل Local-first/Commit/Pending Queue پیروی کنند.

### پرسش ۰۸ — چند دستگاه و دسترسی در آینده

نسخه اول تک‌دستگاه و تک‌نقش است؛ برای آینده باید مدل هویت دستگاه، هویت کاربر و محدوده دسترسی نهایی شود. فعلاً این پرسش فعال نیست و تا رسیدن به مرحله طراحی آن بخش مطرح نمی‌شود.

### پرسش ۰۹ — حل شد

- **به‌روزرسانی اپ (همان دستگاه):** دیتابیس محلی و صف عملیات معلق باید **حفظ شوند**؛ نصب نسخه جدید نباید داده یا صف Pending را پاک کند.
- **تعویض/فرمت گوشی:** بازیابی خودکار دیتابیس/صف از طریق بک‌آپ ابری گوگل (Android Auto Backup) انجام **نمی‌شود**. داده محلی و صف عملیات معلق فقط روی همان دستگاه باقی می‌مانند و با تعویض/فرمت گوشی از بین می‌روند؛ کاربر باید مجدداً اتصال فروشگاه را برقرار کند تا داده از سرور واکشی شود.
- این تصمیم با سیاست امنیتی `SECURITY_AND_AUTH.md` هم‌راستاست: Consumer Key/Secret در هیچ حالتی (نه در بک‌آپ محلی نصب/آپدیت و نه در بک‌آپ ابری) نباید به‌صورت غیرامن منتقل شوند؛ در صورت استفاده از هر مکانیزم بک‌آپ اندرویدی، Credentialهای ذخیره‌شده در Keystore باید صریحاً از دامنه‌ی بک‌آپ مستثنی شوند.

### پرسش ۱۰ — حل شد

در نسخه اول کاربر فقط وضعیت ساده‌ی هر تغییر را می‌بیند: **معلق (Pending) / همگام‌شده (Synced) / ناموفق (Failed)**. تاریخچه کامل تغییرات (Change Log/Audit Trail) به آینده موکول می‌شود و در V1 پیاده‌سازی نمی‌شود.

### پرسش ۱۱ — حل شد (بخش اول)

- **خطای احراز هویت** (مثلاً Consumer Key/Secret نامعتبر): پیام کوتاه به کاربر نمایش داده می‌شود همراه با دکمه‌ی «تلاش مجدد»؛ نیازی به صفحه اختصاصی خطا در V1 نیست.
- **خطای شبکه/موقت (Retry پس‌زمینه):** Retry به‌صورت خاموش/silent در پس‌زمینه انجام می‌شود (بدون نمایش خطای فوری مزاحم). علاوه بر آن، یک **نشانگر کوچک وضعیت اتصال (Status Dot)** بالای صفحه باید همیشه نمایش داده شود که با رنگ وضعیت اتصال/سرعت اینترنت را نشان می‌دهد:
  - 🟢 سبز: اتصال برقرار و همگام
  - 🟡 زرد: اتصال ضعیف/کند یا Sync در حال تلاش مجدد
  - 🔴 قرمز: قطع اتصال یا Sync ناموفق

این نشانگر باید سبک، غیرمزاحم و بدون نیاز به تعامل کاربر باشد و نباید مسیر اصلی UI را کند کند.

**باقی‌مانده (هنوز باز):** نحوه‌ی دقیق نمایش خطاهای سطح دسترسی (Permission)، اعتبارسنجی فرم (Validation)، تعارض (Conflict) و خطای سرور (5xx) هنوز نهایی نشده و باید در پرسش‌های بعدی مشخص شود.

### پرسش ۱۱ — حل شد (بخش دوم، تکمیل)

- **خطای سطح دسترسی (Permission):** کنترل/عملیات مربوطه فقط **پنهان** می‌شود، بدون نمایش پیام خطای جداگانه.
- **خطای اعتبارسنجی فرم (Validation):** خطا **فوراً زیر همان فیلد مشکل‌دار** در فرم نمایش داده می‌شود (نه پیام کلی بالای فرم).
- **Conflict غیرقابل ادغام خودکار:** یک **مودال** به کاربر نمایش داده می‌شود که مقدار Local و مقدار Server را کنار هم نشان می‌دهد و کاربر یکی را انتخاب می‌کند؛ این با تصمیم `CONFLICT_RESOLUTION.md` هم‌راستاست.
- **خطای سرور (5xx):** مانند خطای شبکه، **Retry خاموش در پس‌زمینه** انجام می‌شود و فقط نشانگر کوچک وضعیت اتصال (Status Dot) قرمز/زرد می‌شود؛ پیام مزاحم جداگانه نمایش داده نمی‌شود.

با این تکمیل، پرسش ۱۱ به‌طور کامل حل شده است.

### پرسش ۱۲ — حل شد

سطح آزمایش اجباری V1 **کامل** است و شامل تمام لایه‌های زیر می‌شود:

- **Unit Test:** منطق حیاتی Core از جمله Sync Engine، Entity/Commit Versioning، Conflict Resolution (field-level merge)، Pending Queue.
- **Integration Test:** ارتباط با REST API ووکامرس (شامل Batch API محصولات) و دیتابیس محلی (Room/SQLDelight).
- **UI Test:** جریان‌های اصلی سه محور محصول (اعلان/مشاهده سفارش، مدیریت سفارش، افزودن/ویرایش محصول) روی Jetpack Compose.
- **تست شبکه ناپایدار/کند (الزامی):** رفتار اپ باید صریحاً تحت شرایط زیر آزمایش شود:
  - قطع کامل اینترنت حین انجام تغییر (تغییر باید در Pending Queue بماند و بعداً Retry شود)
  - اتصال کند/ناپایدار (Timeout، Retry با تأخیر، عدم گم‌شدن یا تکرار تغییر)
  - بازگشت اتصال پس از قطعی (Sync خودکار پس‌زمینه‌ای طبق WorkManager)
  - این تست‌ها باید Idempotency عملیات Push را نیز تأیید کنند تا Retry باعث تغییر/محصول/سفارش تکراری نشود.

### پرسش ۱۳ — حل شد

قبل از شروع کدنویسی، تمام صفحات و جریان‌های نسخه اول باید در سطح **Hi-Fi (طراحی مفصل و نزدیک به نسخه نهایی)** طراحی شوند، همراه با یک **Design System کامل** برای تم Liquid Glass (رنگ‌ها، تایپوگرافی، فاصله‌ها، کامپوننت‌های پایه مانند دکمه/کارت/فرم/مودال، حالت‌های RTL/LTR، نشانگر وضعیت اتصال، نمایش خطاهای Validation/Permission/Conflict طبق تصمیمات پرسش ۱۱).

این سطح از طراحی باید پیش از شروع پیاده‌سازی Core و UI آماده باشد تا هسته (Core) و تجربه کاربری از ابتدا هم‌راستا طراحی شوند و در حین کدنویسی نیاز به بازطراحی اساسی UI/UX پیش نیاید.

با این تصمیم، تمام پرسش‌های فهرست `DECISION_TRACKER.md` (۰۱ تا ۱۳) حل شده‌اند.

## قوانین این فهرست

۱. هر بار کاربر به یک پرسش پاسخ می‌دهد، همان پرسش باید در همین فایل `[x]` شود و تصمیم نهایی در سند اصلی مربوط ثبت یا به‌روزرسانی و ارسال شود.
۲. اگر پرسش جدیدی از بررسی معماری، محدودیت رابط برنامه‌نویسی ووکامرس، اندروید یا نیاز محصول به‌وجود آمد، با شماره بعدی اضافه شود.
۳. پرسش تکراری ممنوع است؛ قبل از پرسیدن، این فایل و سند اصلی مرتبط بررسی شود.
۴. اگر پاسخ کاربر باعث تغییر تصمیم قبلی شود، سند قبلی باید به‌روزرسانی شود؛ هیچ چیزی بدون تصمیم صریح حذف نشود.
۵. موارد مسدودکننده باید قبل از شروع پیاده‌سازی هسته تعیین تکلیف شوند.
