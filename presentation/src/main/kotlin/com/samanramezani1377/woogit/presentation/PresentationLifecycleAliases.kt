package com.samanramezani1377.woogit.presentation

/**
 * Package-level alias used by the presentation entry point and factory.
 * Keeps lifecycle's ViewModel type explicit without coupling every file to the
 * AndroidX import name.
 */
typealias ViewModel = androidx.lifecycle.ViewModel
